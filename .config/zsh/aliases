#!/usr/bin/env bash

alias vim="nvim"
export EDITOR="nvim"
export VISUAL="nvim"

# Binds.

# tmux.
# Open tmux sessions with fuzzy finder.
# tmux_sessionizer_widget() {
#   BUFFER="~/.config/bin/tmux-sessionizer"
#   zle accept-line
# }
# zle -N tmux_sessionizer_widget
# bindkey -s '^f' "~/.config/bin/tmux-sessionizer\n"
# bindkey -M vicmd '^f' tmux_sessionizer_widget

# Open Zellij sessions with fuzzy finder.
# bindkey -s '^f' '~/.config/bin/zellij-sessionizer\n'

tmux-sessionizer-widget() {
  BUFFER="~/.config/bin/tmux-sessionizer $BUFFER"
  zle accept-line
}
zle -N tmux-sessionizer-widget
bindkey '^f' tmux-sessionizer-widget
